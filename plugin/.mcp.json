{
  "mcpServers": {
    "topics": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/mcp_tools.py"],
      "env": {
        "TOPICS_DB": "${CLAUDE_PLUGIN_DATA}/topics.db"
      }
    }
  }
}
